/*
 *  EXAMPLE CONFIG FILE
 *  Copy this file and rename to config.js, then customize all options
 */


//Create the container
var conf = {
    'http-port': 'Enter the port as a plain number',
    'https-port': 'Enter the port as a plain number'
};

//Export the container
module.exports = conf;